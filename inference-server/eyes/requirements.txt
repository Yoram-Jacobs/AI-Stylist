# DressApp Eyes (Gemma-4 E2B GGUF) — CPU-only Hetzner deploy.
#
# llama-cpp-python: Gemma-3 / Gemma-4 E2B architecture support landed
# after 0.3.5 (Dec 2024). 0.3.19 is the latest stable release with a
# prebuilt CPU wheel published in abetlen's wheel index. 0.3.16 was
# tried first but has NO published wheel — pip fell back to a source
# build that needed gcc/cmake. The Dockerfile now ships the toolchain
# anyway as a safety net (so a future wheel-index gap won't break us).
llama-cpp-python==0.3.19
fastapi==0.115.6
uvicorn[standard]==0.32.1
pydantic==2.10.4
huggingface_hub==0.27.0
# Pillow is only consumed by the Phase-2 Llava chat handler (vision).
# Keeping it here so an mmproj swap in the future doesn't need a
# requirements bump on the deployed VPS.
Pillow==11.0.0
